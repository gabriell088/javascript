/*
qual a diferença entre let,const e var em JavaScript?
*/
var Escopo de função: Uma variável declarada com var tem escopo de função. Isso significa que ela é visível em toda a função em que foi declarada.
let Escopo de bloco: Uma variável declarada com let tem escopo de bloco. Isso significa que ela só é visível dentro do bloco em que foi declarada.
const alor constante: Uma vez atribuído um valor a uma variável const, esse valor não pode ser alterado ou reatribuído. No entanto, se a variável const contiver um objeto ou array, os membros desse objeto ou elementos do array podem ser modificados.